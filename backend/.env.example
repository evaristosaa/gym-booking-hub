# Never commit the real values. Generate a Fernet key with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
GYM_BOOKING_ENCRYPTION_KEY=

# Comma-separated HTTPS origins allowed to call the API.
# Add the final GitHub Pages origin before production deployment.
GYM_BOOKING_ALLOWED_ORIGINS=https://bailongo-pc-1.tail1e5afd.ts.net:8445
